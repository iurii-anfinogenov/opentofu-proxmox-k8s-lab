# OpenTofu Proxmox Kubernetes Lab

Учебный проект для [курса по инфраструктуре](https://stepik.org/course/238385/promo) как коду (IaC) и автоматизации развёртывания Kubernetes-кластера. Репозиторий демонстрирует полный путь: от модульного OpenTofu-кода, создающего виртуальные машины в Proxmox, до подготовки базового кластера Kubernetes.

Проект создан как практическое дополнение к модулю курса об OpenTofu/Terraform, и служит фундаментом для дальнейшей автоматизации с помощью Ansible.

## 📦 Что содержит проект

* Модуль OpenTofu для создания Kubernetes-нод в Proxmox.
* Генерацию cloud-init конфигураций.
* Параметризованную выдачу VMID, IP, hostname.
* Готовый root-конфиг, использующий модуль `k8s-node`.
* Возможность добавлять и удалять конкретные ноды без count.
* Подготовку виртуальных машин к установке Kubernetes.

## 📁 Структура репозитория

```
├── README.md
├── locals.tf
├── main.tf
├── modules
│   └── k8s-node
│       ├── cloud-config
│       ├── locals.tf
│       ├── main.tf
│       ├── outputs.tf
│       └── variables.tf
├── outputs.tf
├── providers.tf
├── terraform.tfstate
├── terraform.tfstate.backup
├── terraform.tfvars
└── variables.tf
```

## 🚀 Быстрый старт

### 1. Инициализация проекта

```
tofu init
```

### 2. Проверка плана

```
tofu plan
```

### 3. Создание инфраструктуры

```
tofu apply
```

После выполнения этой команды Proxmox создаст виртуальные машины, сгенерирует userdata для cloud-init и развернёт требуемые ноды.

## ⚙️ Управление нодами

Модуль принимает объект вида:

```
nodes = {
  master1 = { role = "master", cpu = 2, memory = 4096 }
  worker1 = { role = "worker", cpu = 2, memory = 4096 }
  worker2 = { role = "worker", cpu = 2, memory = 4096 }
}
```

Вы можете:

* Добавить новую ноду, просто вписав её в map.
* Удалить ноду, удалив её ключ из map.
* Иметь несколько кластеров, копируя модуль в разные окружения.

## 🧩 Следующие шаги

Этот модуль создаёт серверную часть, но настройку Kubernetes внутри ВМ мы делаем вручную — так учащийся лучше понимает, как работает кластер.

В следующем курсе по **Ansible** мы:

* Полностью автоматизируем установку Kubernetes.
* Добавим установку сетевых плагинов.
* Настроим ingress-контроллер.
* Расширим модуль, чтобы кластеры создавались полностью автоматически.

Ссылка на курс по Ansible будет добавлена позже.

## 📚 Полезная информация

Проект подходит как основа для:

* локальных Kubernetes-лабораторных стендов,
* тестовых dev‑кластеров,
* демонстрации модульного подхода в Infrastructure as Code.

Проект развивается вместе с курсом, поэтому структура может расширяться.

---

Если вы проходите курс, обязательно загрузите изменения в свой GitHub и продолжайте работу в следующих уроках — это поможет выстроить системное понимание IaC и автоматизации.
